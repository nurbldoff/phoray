from __future__ import division

from math import *


cdef class Vector:

    cdef public double x, y, z

    # cdef public Vector norm(self)

    # cdef public Vector normalized(self)

    # cdef public Vector dot(self, Vector v)

    # cdef public Vector cross(self, Vector v)

    # cdef Vector _rotate(self, Vector theta)

    # cdef public Vector rotate(self, Vector rotvec)

    # cdef public Vector rotate_around(self, Vector axis, double angle)
